<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:paddingEnd="12dp"
    android:paddingStart="12dp">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:background="@null"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/pop_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:padding="10dp"
            android:text="@string/cancel"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/pop_ok"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:padding="10dp"
            android:text="@string/sure"
            android:textSize="15sp" />
    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/c_eaeaea"
        android:paddingTop="10dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/wheel_picker_province"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:wheel_indicator="true"
            app:wheel_indicator_color="@color/c_eaeaea"
            app:wheel_indicator_size="1dp"
            app:wheel_item_text_size="16sp"
            app:wheel_visible_item_count="6"/>

        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/wheel_picker_city"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:wheel_indicator="true"
            app:wheel_indicator_color="@color/c_eaeaea"
            app:wheel_indicator_size="1dp"
            app:wheel_item_text_size="16sp"
            app:wheel_visible_item_count="6"/>

        <com.aigestudio.wheelpicker.WheelPicker
            android:id="@+id/wheel_picker_district"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:wheel_indicator="true"
            app:wheel_indicator_color="@color/c_eaeaea"
            app:wheel_indicator_size="1dp"
            app:wheel_item_text_size="16sp" />
    </LinearLayout>
</LinearLayout>
